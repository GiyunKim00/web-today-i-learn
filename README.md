# Today I Learn 페이지 만들기
## 0. 프로젝트 소개
> `우아한테크코스 8기 공통강의 - web`의 페이지 만들기 실습입니다.
---
## 1. 기술 스택

- HTML5
- CSS3
- JavaScript
---
## 2. 요구사항
### 2-1. 기능 요구사항

### 2-1-1. 프로필 영역
- [x] 프로필 이미지를 `<img>` 태그로 구성한다
- [x] 닉네임(이름)을 표시한다
- [x] 한 줄 소개를 작성한다
- [x] 기본 정보를 `<table>`로 구성한다
  - [x] 이름을 포함한다
  - [x] MBTI를 포함한다
  - [x] 취미를 포함한다
  - [x] 좋아하는 음식을 포함한다
- [x] 자기소개를 `<p>` 태그로 작성한다
- [x] 소개 글 안에 `<a>` 태그를 사용해 외부 링크를 연결한다
- [x] GitHub 또는 블로그 링크를 포함한다

### 2-1-2. 갤러리
- [x] 갤러리 부제를 작성한다
- [x] 9장의 이미지를 배치한다
- [x] 이미지들을 정사각형으로 표현한다

### 2-1-3. 인생 작품
- [x] 최소 2개의 작품을 소개한다
- [x] 목록 형태`<ul>`, `<ol>`로 구성한다
- [x] 작품 설명을 작성한다
- [x] 작품 중 하나에 `<video>`, `<iframe>`을 사용해 영상을 삽입한다

### 2-1-4. TIL
- [x] TIL 섹션을 구성한다
- [x] 날짜를 포함한다
- [x] 학습 주제를 포함한다
- [x] 학습 내용을 포함한다

### 2-1-5. 상단 내비게이션
- [x] `<a>` 태그를 사용해 페이지 내 이동 링크를 구성한다
- [x] 각 섹션으로 이동 가능하도록 anchor를 설정한다

### 2-1-6. CSS
- [x] CSS 파일을 별도로 분리한다
- [x] `<link>` 태그로 연결한다
- [x] 태그 선택자 사용
- [x] 클래스 선택자 사용
- [x] CSS Grid 또는 Flexbox를 활용한다
- [x] 9장의 이미지를 균형 있게 배치한다
- [x] 템플릿을 기반으로 자유롭게 스타일을 적용한다
- [x] 카드 UI / 여백 / 색상 등을 개선한다

### 2-1-7. JS
- [x] JavaScript 파일을 별도로 분리한다
- [x] `<script>` 태그로 연결한다
- [x] `<form>` 태그 구성
- [x] 날짜 `<input>` 추가
- [x] 제목 `<input>` 추가
- [x] 내용 `<textarea>` 추가
- [x] 모든 입력 요소에 `<label>` 연결
- [x] 등록 버튼 클릭 시 이벤트 발생
- [x] 입력 데이터를 받아 새로운 TIL 생성
- [x] 목록에 추가
- [x] `document.querySelector()` 사용
- [x] DOM 요소 선택 및 제어

## 2-2. 프로그래밍 요구사항

### 2-2-1. 기본 설정
- [x] HTML 인코딩을 UTF-8로 설정한다 (`<meta charset="utf-8">`)
- [x] viewport 설정을 추가한다
- [x] `<title>` 태그를 설정한다
- [x] og:title을 설정한다
- [x] og:description을 설정한다
- [x] og:image를 설정한다
- [x] `<h1>` ~ `<h6>`를 사용하여 계층 구조를 구성한다
- [x] 모든 `<img>` 태그에 alt 속성을 작성한다

### 2-2-2. 시멘틱 태그
- [x] `<header>`를 사용한다
- [x] `<nav>`를 사용한다
- [x] `<main>`을 사용한다
- [x] `<section>`을 사용한다
- [x] `<article>`을 사용한다
- [x] `<footer>`를 사용한다

### 2-2-3. 제한사항
- [x] 리소스를 상대 경로로 불러온다
- [x] images 디렉터리에는 이미지 파일만 저장한다
- [x] css 디렉터리에는 스타일 파일만 저장한다
- [x] root 디렉터리에는 웹 문서만 저장한다